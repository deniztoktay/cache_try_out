package tech.pardus.newdesign.cachekey;

/** Stable identifier used to resolve a {@link CacheKey} component from {@link CacheKeyFactory}. */
public enum ApplicationCache {
  TAG,
  ATTRIBUTE,
  ATTRIBUTE_SETTING,
  ATTRIBUTE_TAG,
  REFERENCE_TYPE
}
